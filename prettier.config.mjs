/** @type {import('prettier').Config} */
const config = {
    semi: true,
    singleQuote: true,
    trailingComma: 'all',
    tabWidth: 4,
    useTabs: false,
    printWidth: 120,
    bracketSpacing: true,
    jsxSingleQuote: false,
};

export default config;
