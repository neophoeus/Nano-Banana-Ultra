內部 smoke 已完成，App-Nano_Banana_Ultra 目前可繼續作為內部開發與內部 smoke 使用；這次已修掉兩個實際問題：一是 restored fake queued jobs 造成的 invalid batch name 反覆 auto-poll，現在會正確轉成 terminal failed state，不再持續噴輪詢噪音；二是 SketchPad 在初始 auto-fit 後因為 component 內部用 scale、共用 viewport helper 用 zoom 而導致的 React controlled/uncontrolled warning，現在已統一修正並補上回歸測試。修復後 focused 測試已通過 46 passed / 506 passed，live app 也補做了完整 restore 分支驗證：`Workspace Restored -> Open latest turn -> Open viewer -> Close`、`Workspace Restored -> Continue source`、`Workspace Restored -> Branch from restored turn` 都已實際走通，並確認 stage source / follow-up source 會正確切到 `History · Reopen`、`History · Continue`、`History · Branch`。另外，queued-import restore fixtures 與 shared workspace snapshot 已改成 inline data URL，不再依賴 `example.com` 假圖，因此這條 smoke 路徑原本的外部載入噪音已移除。剩餘項目主要只剩 fixture 層級的非阻塞噪音，例如 restore fixture 的假 queued job 仍會在 queue panel 顯示為歷史失敗項；若要做更乾淨 demo，下一步應優先整理 fixture 呈現，而不是產品功能層修 bug。
